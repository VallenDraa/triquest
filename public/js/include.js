// // making sure height is the same at all difficulty cards
// const leaderboard = document.querySelector('.leaderboard');
// const rankHeader = document.querySelector('.rank-header');
// const rank = document.querySelectorAll('.rank');

// window.addEventListener('resize', () => {
//   // calculate the width percentage for the rank cell in table
//   console.log(leaderboard.offsetWidth);
//   console.log(rankHeader.offsetWidth);

//   rank.forEach((item) => {
//     item.style.width = `${
//       (rankHeader.offsetWidth / leaderboard.offsetWidth) * 100
//     }%`;
//   });
// });
