const signUpBtnProfile = document.getElementById('signup-btn-profile');
const loginBtnProfile = document.getElementById('login-btn-profile');

signUpBtnProfile.addEventListener('click', function () {});

loginBtnProfile.addEventListener('click', function () {});
